# Package initialization - avoid duplicating FastAPI app instance
# The main FastAPI app is created and configured in app.main

# This file is intentionally minimal to prevent duplicate route registrations
# All configuration is centralized in app.main.py